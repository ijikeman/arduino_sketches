#include "MatrixController.h"
#include "PinController.h"
#include "Arduino.h"

MatrixController::MatrixController(unsigned int * p_auiOutputPinNumbers, unsigned int uiOutputPinNumbersOf, unsigned int * p_auiInputPinNumbers, unsigned int uiInputPinNumbersOf) {
  // m_uiOutputPinNumbersOf = uiOutputPinNumbersOf;
  // m_uiInputPinNumbersOf = uiInputPinNumbersOf;
  // m_p_auiOutputPinNumbers = p_auiOutputPinNumbers;
  // m_p_auiInputPinNumbers = p_auiInputPinNumbers;

  // for (unsigned int i = 0; i < m_uiOutputPinNumbersOf; i++) {
  //   p_acOutputPins[i] = &PinController(p_auiOutputPinNumbers[i], HIGH, OUTPUT);
  // }
  // for (unsigned int i = 0; i < m_uiInputPinNumbersOf; i++) {
  //   p_acInputPins[i] = &PinController(p_auiInputPinNumbers[i], HIGH, INPUT);
  // }
}

bool MatrixController::scan() {
  // for (m_uiOutputCurrentNumber; m_uiOutputCurrentNumber < m_uiOutputPinNumbersOf; m_uiOutputCurrentNumber++) {
  //   for (m_uiOutputCurrentNumber; m_uiInputCurrentNumber < m_uiInputPinNumbersOf; m_uiInputCurrentNumber++) {

  //   }
  // }
}
